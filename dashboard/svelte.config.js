// svelte.config.js — Configuração do SvelteKit para o Dashboard Cascata.
// Implementação será expandida conforme o dashboard for desenvolvido.
/** @type {import('@sveltejs/kit').Config} */
const config = {
    kit: {
        // adapter será configurado quando o dashboard for desenvolvido
    }
};

export default config;
