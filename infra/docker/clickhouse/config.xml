<!-- =================================================================== -->
<!-- ClickHouse — Configuração Cascata (modo Shelter / single node)     -->
<!-- Ref: SAD §C (Observabilidade), TASK PR-7                          -->
<!-- =================================================================== -->
<clickhouse>

    <!-- Logging -->
    <logger>
        <level>warning</level>
        <console>true</console>
    </logger>

    <!-- Limites de query mudados para o users.xml ou omitidos por default -->

    <!-- Limites de query mudados para o users.xml ou omitidos por default -->

    <!-- Network -->
    <listen_host replace="1">0.0.0.0</listen_host>
    <listen_try>1</listen_try>
    <interserver_listen_host replace="1">0.0.0.0</interserver_listen_host>

    <!-- Performance tuning para single node (Shelter: 384MB cgroup) -->
    <max_concurrent_queries>20</max_concurrent_queries>
    <background_pool_size>2</background_pool_size>
    <background_schedule_pool_size>2</background_schedule_pool_size>

    <!-- Merge tree settings -->
    <merge_tree>
        <max_bytes_to_merge_at_max_space_in_pool>536870912</max_bytes_to_merge_at_max_space_in_pool>
        <number_of_free_entries_in_pool_to_lower_max_size_of_merge>0</number_of_free_entries_in_pool_to_lower_max_size_of_merge>
        <number_of_free_entries_in_pool_to_execute_mutation>0</number_of_free_entries_in_pool_to_execute_mutation>
        <number_of_free_entries_in_pool_to_execute_optimize_entire_partition>0</number_of_free_entries_in_pool_to_execute_optimize_entire_partition>
    </merge_tree>

    <!-- Timezone -->
    <timezone>UTC</timezone>

</clickhouse>
